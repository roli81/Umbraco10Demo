@model Sss.Umb9.Mutobo.Interfaces.ITheme
<style>

    html {
        --background-color: @($"#{Model.BackgroundColor}");
        --color-primary: @($"#{Model.Color}");
        --header-background-color: @($"#{Model.HeaderBackgroundColor}");
        --footer-background-color: @($"#{Model.FooterBackgroundColor}");
        --navigation-background-color: @($"#{Model.NavigationBackgroundColor}");
        --navigation-background-color-hover: @($"#{Model.NavigationBackgroundColorHover}");
        --navigation-color-hover: @($"#{Model.NavigationColorHover}");
        --footer-link-color: @($"#{Model.FooterColor}");
        --footer-link-color-hover: @($"#{Model.FooterColorHover}");
        --navigation-color: @($"#{Model.NavigationColor}");
        
        
        
    }
</style>